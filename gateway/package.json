{
  "name": "konkred-gateway",
  "version": "2.0.0",
  "description": "Konkred Free-API Maximizer v2 — quota-aware multi-provider LLM gateway (zero runtime dependencies)",
  "type": "module",
  "main": "src/server.mjs",
  "private": true,
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "MIT"
}
